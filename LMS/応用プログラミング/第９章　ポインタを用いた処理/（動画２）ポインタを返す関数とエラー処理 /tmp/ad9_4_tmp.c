#include <stdio.h>

/* BLANK */ {
    int i;

    for (i = 0; i < size; i++) {
        if (/* BLANK */) {
            return (/* BLANK */);
        }
    }
    return (/* BLANK */);
}

int main(void) {
    int data[] = {10, 20, 30, 40, 50};
    /* BLANK */

    /* BLANK */

    if (/* BLANK */) {
        printf("Not found\n");
    } else {
        printf("Found: %d\n", *p);
    }

    return (0);
}