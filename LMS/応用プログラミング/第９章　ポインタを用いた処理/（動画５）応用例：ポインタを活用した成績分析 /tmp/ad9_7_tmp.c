#include <stdio.h>

/* BLANK */ {
    int i, sum = 0;

    /* BLANK */;
    /* BLANK */;

    for (i = 0; i < size; i++) {
        sum += scores[i];
        if (/* BLANK */) {
            /* BLANK */;
        }
        if (/* BLANK */) {
            /* BLANK */;
        }
    }
    /* BLANK */
}

int main(void) {
    int scores[] = {88, 76, 92, 64, 80};
    int size = sizeof(scores) / sizeof(scores[0]);
    /* BLANK */
    int max_score, min_score;

    /* BLANK */

    printf("Average: %.2f\n", average);
    printf("Max score: %d\n", max_score);
    printf("Min score: %d\n", min_score);

    return (0);
}
