#include <stdio.h>
#include <string.h>
// Define a structure for a student
struct student {
    ア
    char name [20];
    double score;
};
int main(void) {
    // Declare a student variable
    イ
    // Access and assign values to members
    sl.id = 101;
    strcpy(s1.name, "Alice");
    ウ
    // Display the values of members
    I
    オ
    printf("Student Score: %.1f\n", s1.score);
    return(0);
}