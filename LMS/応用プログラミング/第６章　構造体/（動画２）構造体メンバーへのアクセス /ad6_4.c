#include <stdio.h>
// Define a structure for student information
typedef struct student {
    char name [50];
    int id;
    int grade;
} Student;

int main(void) {
    // Declare a student variable
    Student studenti;

    // Input values for members

    printf("Enter student name:"); scanf("%49s", student1.name);
    printf("Enter student ID: "); scanf("%d", &student1.id);
    printf("Enter student grade: ");
    scanf("%d", &student1.grade);

    return (0);
}